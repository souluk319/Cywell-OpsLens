# MVP 0.1 Acceptance Matrix

Scope: console-shaped read-only prototype for Cywell OpsLens, with fixture-backed assistant contracts, a live OpenShift API validation lane, a Stage 1 OpenShift Lightspeed custom MCP integration contract, and the first Stage 2 alert-triggered incident analysis contract.

## Locked Goal

Prove that a dashboard-first, context-aware OpenShift operations assistant can stay out of the way of evidence while producing an evidence-first read-only analysis contract.

## Pass/Fail Criteria

| ID | Source criterion | Pass | Measurement | Evidence | Current gap |
|---|---|---|---|---|---|
| AC-UI-001 | UI 가림 방지 | Alerts page shows severity, count, and status while the lower-right assistant popover is open. | Playwright visibility and bounding-box non-overlap check with screenshot. | `test-results/ac-ui-001-alerts-non-occluding.png` | Mock console route only. |
| AC-UI-002 | Lightspeed-style assistant surface | Assistant starts as a lower-right launcher, opens as a popover on click, closes from the popover, and does not resize the console workspace. | Playwright launcher/popover state and workspace bounding-box assertions. | `assistant-launcher`, `assistant-popover`, `aria-expanded`. | Popover is prototype-local until implemented as an OpenShift Console Plugin extension. |
| AC-CTX-001 | Context sync | Route, namespace, resource, filters, visible rows are captured. | Payload snapshot assertion. | Rendered `context-payload` JSON. | Fixture-backed until Console context publisher exists. |
| AC-ANS-001 | RAG citation / answer layout | Judgment, evidence, candidates, next checks, risks, rollback, citations, missing evidence render as required blocks. | DOM assertions against answer contract. | Assistant answer blocks. | Mock answer until RAG/API exists. |
| AC-SAFE-001 | Read-only safety | L0 explain, L1 read, and L2 plan only; no apply/delete/scale commands. | DOM assertions and command text scan. | `actionMode=readOnly`. | Policy enforcement API is Phase 1. |
| AC-API-001 | Backend API contract | Dashboard, context sync, and action plan endpoints return typed mock contracts with request id, context hash, answer, and audit envelope. | Playwright API assertions through Vite proxy. | `/api/dashboard/risks`, `/api/context/sync`, `/api/actions/plan`. | Mock backend until OpenShift API/RBAC service exists. |
| AC-LS-001 | Lightspeed MCP Trojan Horse | Cywell OpsLens exposes a read-only MCP tool catalog and tool-call endpoint for Lightspeed, returns customer-runbook grounded answers through private RAG policy, redacts sensitive values, excludes `apply_remediation`, and marks MCP as Technology Preview. | Playwright API assertions against `/api/opslens/tools`, `/api/opslens/ask`, and JSON-RPC `tools/list` + `tools/call` through `/api/opslens/mcp`; production-facing endpoint is `/mcp`. | `generate_playbook`, customer-runbook citations, `rawDocumentReturned=false`, `mutationAllowed=false`, `mcpTechnologyPreview=true`, audit envelope. | Real vLLM/vector DB and live OLSConfig deployment are next Phase 1 implementation lanes. |
| AC-LS-002 | Lightspeed MCP live readiness | Cywell OpsLens validates OpenShift Lightspeed MCP readiness before any OLSConfig mutation. | `npm run verify:lightspeed` reads the live `olsconfigs.ols.openshift.io` CRD, validates `spec.featureGates` and `spec.mcpServers`, checks the template shape, and optionally proves `tools/list` + `tools/call` with `--mcp-url` or `CYWELL_OPSLENS_MCP_URL`. | Pass/fail/warn verifier output; no cluster apply; fixture parity through `npm run verify:lightspeed:fixture`; hard fail if the CRD lacks MCP schema or `--require-mcp` is set without a working MCP endpoint. | Fixture verification exists locally; live cluster execution still depends on customer or lab OpenShift Lightspeed access. |
| AC-AIOPS-001 | Alert-triggered incident packet | Cywell OpsLens turns an alert into a plan-only incident packet that combines read-only OCP resource detail, pod candidates, events, recent pod logs, private runbook citations, structured YAML remediation proposal, missing evidence, risks, and rollback path. | Playwright API assertion against `POST /api/opslens/incidents/analyze` after discovering a live pod through `/api/ocp/resources`; pod logs are requested with `sinceSeconds=600`; remediation proposal is asserted as a non-mutating review artifact. | `actionMode=planOnly`, `policy.mutationAllowed=false`, `policy.serverSideRedaction=true`, redacted log/event evidence, `analysis.policy.rawDocumentReturned=false`, `analysis.audit.sources`, `analysis.remediationProposal.artifactType=opslens.remediation.proposal.v0.1`, `remediationProposal.mutationAllowed=false`, `remediationProposal.patchType=strategicMerge`, memory proposal `proposedValue=4Gi`, `forbiddenActions=[apply,delete,scale]`, `reviewGate.required=true`, `missingEvidence`, and SelfSubjectAccessReview evidence. | This slice is still API-first; dashboard visualization of the structured proposal follows later. |
| AC-AIOPS-002 | Prometheus metric correlation | Incident analysis adds alert, restart, CPU, and memory Prometheus queries when monitoring proxy is enabled, and records explicit missing evidence when it is disabled or unreachable. | Playwright API assertion against the `metrics` block returned by `POST /api/opslens/incidents/analyze`; the same endpoint is valid with `OCP_ENABLE_MONITORING_PROXY=true` or false. | `metrics.windowMinutes=10`, query names include `firing-alert`, `pod-restarts`, `pod-cpu`, `pod-memory`; `policy.monitoringProxyEnabled` mirrors `metrics.enabled`; disabled/unreachable monitoring is surfaced as metric `missingEvidence` instead of hidden. | Full Prometheus charting belongs to the dedicated OpsLens dashboard. |
| AC-RAG-001 | Tenant-scoped local vector RAG | Cywell OpsLens indexes tenant Markdown runbooks into a deterministic local vector index, retrieves customer runbook citations without returning raw documents, redacts sensitive query values, and blocks cross-tenant retrieval. | `npm run verify:rag` builds `@kugnus/rag`, indexes `data/runbooks`, runs payments and Secret-checklist retrievals, and checks unknown-tenant isolation. | `packages/rag/src/localIndex.ts`, `scripts/verify-rag-index.mjs`, `local-vector-v0.1`, `tenantScoped=true`, `rawDocumentReturned=false`, `serverSideRedaction=true`, chunk-level evidence. | This is a local hash-vector store; production Qdrant/pgvector ingestion and live vLLM embeddings remain later implementation lanes. |
| AC-RAG-002 | Validate-only RAG document intake | Cywell OpsLens accepts operator-authored Markdown runbook drafts only for validation, returns metadata-only document/chunk previews, redacts sensitive-looking text, rejects duplicate tenant document IDs, exports an audit-safe validation artifact, and never applies the upload in MVP 0.1. | `npm run verify:rag` calls `validateRagDocumentIntake` and `createRagValidationEvidenceExport` with a safe draft containing sensitive-looking text and a duplicate existing runbook ID. Playwright also asserts `POST /api/opslens/admin/rag/validate`, `POST /api/opslens/admin/rag/evidence-export`, and the dashboard validation/export panel. | `actionMode=validateOnly`, `policy.uploadApplyAllowed=false`, `rawDocumentReturned=false`, redacted chunk snippets, `sensitive-text` warning, `duplicate-document-id` failure, `opslens.rag.validation-evidence.v0.1`, `approvalQueue.mode=designOnly`, `enqueueAllowed=false`, dashboard `opslens-rag-validation`. | This validates and exports evidence only; durable ingestion, approval queue persistence, and vector DB writes are later lanes. |
| AC-DASH-001 | Dedicated OpsLens admin dashboard | Cywell OpsLens exposes a dashboard surface separate from Lightspeed chat for RAG document health, validate-only RAG draft checks, evidence export, token usage, GPU/runtime samples, incident metric visualization, plan-only remediation review, and install readiness. | Playwright API + UI assertions against `GET /api/opslens/admin/overview`, `POST /api/opslens/admin/rag/validate`, `POST /api/opslens/admin/rag/evidence-export`, and rendered `opslens-admin-dashboard`. | RAG documents are metadata-only and redacted, validation returns `validateOnly`, evidence export returns `markdownReturned=false` and `enqueueAllowed=false`, token usage has route-level budgets, GPU samples render, incident metric query statuses render, `opslens-remediation-proposal` renders YAML `memory: 4Gi` with `mutationAllowed=false` and `reviewGate=true`, policy blocks mutation/upload apply/raw document return. | Upload is validation/export-only; real document ingestion workflow is a later dashboard lane. |
| AC-OP-001 | Operator packaging skeleton | Cywell OpsLens defines an explicit Operator install contract for API, dashboard, vector store, model runtime, validate-only RAG document intake, design-only RAG approval queue, ConsolePlugin, opt-in Lightspeed MCP registration, and a Go/controller-runtime manager source path for explicit OLSConfig patching. | `npm run verify:operator` parses Operator YAML, validates CRD/sample/CSV/RBAC/bundle metadata/app manifests, checks `ValidateOnly` default, explicit `PatchOLSConfig` sample registration, RAG `rawDocumentReturnAllowed=false`, approval `enqueueAllowed=false`, denies Operator raw Secret read RBAC, and statically checks `deploy/operator/controller-runtime/**` including the OLSConfig patch source. | `deploy/operator/config/**`, `deploy/operator/bundle/**`, `deploy/operator/controller-runtime/**`, `OpsLensInstallation.spec.rag`, `cywell-opslens-rag-policy` ConfigMap, API RAG safety env, `ClusterServiceVersion`, `ConsolePlugin`, managed `OLSConfig` MCP URL ending `/mcp`, RBAC for `olsconfigs`, `consoleplugins`, service account reconciliation, and no `secrets get/list/watch`. | Go/Operator SDK local compile/test, live OLM install/upgrade/uninstall smoke, image builds, and external certification pipeline execution are next Stage 4 lanes. |
| AC-OP-002 | Operator reconcile safety core | Cywell OpsLens controller logic separates non-mutating `ValidateOnly` from explicit `PatchOLSConfig`, preserves existing Lightspeed config state, emits rollback path/evidence, keeps assistant actions plan-only, renders RAG approval queue policy as design-only/non-mutating, and mirrors that contract in controller-runtime source. | `npm run verify:operator:reconcile` builds `@kugnus/operator-controller` and runs fixture assertions against base, registered, missing, and validate-only OLSConfig states plus RAG policy rendering. `npm run verify:operator:runtime` compares the TypeScript desired resource plan with the Go/controller-runtime source for ServiceAccount, Service, runtime ports, vector persistence, ConsolePlugin proxy, RAG env/status safety, OLSConfig patch path, and RBAC parity. | `packages/operator-controller/src/reconcile.ts`, `deploy/operator/controller-runtime/controllers/opslensinstallation_controller.go`, `deploy/operator/fixtures/*.yaml`, `scripts/verify-operator-runtime-parity.mjs`, `strategicMergePatch.spec.featureGates`, preserved existing MCP servers, Cywell `/mcp` registration, Go `client.MergeFrom(original)` patch, rollback annotation, `mutationAllowed=false` for ValidateOnly, `ragApprovalQueueMutationAllowed=false`, `ragRawDocumentReturnAllowed=false`, `MissingOLSConfig` blocks patching, authorized `ConsolePlugin` API proxy. | Go source is statically verified but not locally compiled because Go/Operator SDK are unavailable; live Operator SDK runtime and OLM install smoke remain Stage 4 follow-up. |
| AC-CERT-001 | Catalog and certification readiness draft | Cywell OpsLens prepares internal catalog, FBC, Manual Subscription, scorecard config, Red Hat-oriented annotations, support matrix, security readiness, and release gates without claiming certification has passed. | `npm run verify:certification` parses CSV/bundle metadata/FBC/CatalogSource/Subscription/scorecard/docs and reports missing external CLIs or placeholders as warnings. | `deploy/catalog/**`, `deploy/operator/bundle/tests/scorecard/config.yaml`, CSV `relatedImages`, `com.redhat.openshift.versions`, `features.operators.openshift.io/*`, support/security/release docs, `installPlanApproval: Manual`. | `opm`, `operator-sdk`, container certification, partner portal submission, image scanning, and live certification pipeline execution are external follow-up gates. |
| AC-OCP-001 | Live OCP read-only discovery | API discovers live OpenShift resources, renders console-like overview, proves read access with `SelfSubjectAccessReview`, exposes `get/list/watch` matrix, generates bounded/full resource coverage matrices with gap classification, diagnoses CRD/APIService/conversion-webhook failures, probes alternate served API versions, exports evidence, lists paginated and label/field selector-scoped pods, supports namespace selection, reads sanitized pod detail as JSON/YAML, resolves owner/child relationships, reads pod logs/events, and blocks raw Secret fetch. | Playwright API + UI assertions against configured CRC/OpenShift local, including list/detail fallback when diagnostic finds a readable alternate served version. | `/api/ocp/status`, `/api/ocp/console-overview`, `/api/ocp/api-resources`, `/api/ocp/access-review`, `/api/ocp/access-matrix`, `/api/ocp/coverage-matrix`, `/api/ocp/coverage-diagnostic`, `/api/ocp/resources?labelSelector=...&fieldSelector=...`, `/api/ocp/resource`, `/api/ocp/related`, `/api/ocp/pod-logs`, `/api/ocp/events`; fallback response blocks with requested/served API version and evidence; UI coverage matrix, coverage diagnostic panel, full scan control, evidence export, object JSON/YAML, selector inputs, and related resource panels. | Monitoring proxy is opt-in; bounded coverage reports skipped resources as `not-probed`; cluster API/webhook failures remain visible as classified diagnostic gaps; readable alternate versions are evidence-backed fallbacks, not hidden repairs; mutating verbs and raw Secrets remain out of scope. |

## Explicit Non-Goals

- No automatic `apply`, `delete`, or `scale`.
- No Kubernetes Secret fetch.
- No external LLM provider call from the prototype.
- No full Operator controller lifecycle yet; MVP 0.1 includes package contract, controller-runtime source with explicit OLSConfig patch path, TypeScript reconcile contract, and static verifier.
- No production RAG/vector database yet; Stage 1 exposes the contract, local Markdown private-RAG policy path, validate-only intake checks, and evidence export artifact.

## Verification Command

```bash
npm run verify:mvp
npm run build
npm run test:e2e
npm run verify:rag
npm run verify:operator
npm run verify:operator:reconcile
npm run verify:operator:runtime
npm run verify:certification
npm run verify:lightspeed:fixture
```
