# MVP 0.1 Acceptance Matrix

Scope: console-shaped read-only prototype for Cywell OpsLens, with fixture-backed assistant contracts, a live OpenShift API validation lane, a Stage 1 OpenShift Lightspeed custom MCP integration contract, and the first Stage 2 alert-triggered incident analysis contract.

## Locked Goal

Prove that a dashboard-first, context-aware OpenShift operations assistant can stay out of the way of evidence while producing an evidence-first read-only analysis contract.

## Pass/Fail Criteria

| ID | Source criterion | Pass | Measurement | Evidence | Current gap |
|---|---|---|---|---|---|
| AC-UI-001 | UI 가림 방지 | Alerts page shows severity, count, and status while the lower-right assistant popover is open. | Playwright visibility and bounding-box non-overlap check with screenshot. | `test-results/ac-ui-001-alerts-non-occluding.png` | Mock console route only. |
| AC-UI-002 | Lightspeed-style assistant surface | Assistant starts as a lower-right launcher, opens as a popover on click, closes from the popover, and does not resize the console workspace. | Playwright launcher/popover state and workspace bounding-box assertions. | `assistant-launcher`, `assistant-popover`, `aria-expanded`. | Popover is prototype-local until implemented as an OpenShift Console Plugin extension. |
| AC-CTX-001 | Context sync | Route, namespace, resource, filters, visible rows are captured. | Payload snapshot assertion. | Rendered `context-payload` JSON. | Fixture-backed until Console context publisher exists. |
| AC-ANS-001 | RAG citation / answer layout | Judgment, evidence, candidates, next checks, risks, rollback, citations, missing evidence render as required blocks. | DOM assertions against answer contract. | Assistant answer blocks. | Mock answer until RAG/API exists. |
| AC-SAFE-001 | Read-only safety | L0 explain, L1 read, and L2 plan only; no apply/delete/scale commands. | DOM assertions and command text scan. | `actionMode=readOnly`. | Policy enforcement API is Phase 1. |
| AC-API-001 | Backend API contract | Dashboard, context sync, and action plan endpoints return typed mock contracts with request id, context hash, answer, and audit envelope. | Playwright API assertions through Vite proxy. | `/api/dashboard/risks`, `/api/context/sync`, `/api/actions/plan`. | Mock backend until OpenShift API/RBAC service exists. |
| AC-LS-001 | Lightspeed MCP Trojan Horse | Cywell OpsLens exposes a read-only MCP tool catalog and tool-call endpoint for Lightspeed, returns customer-runbook grounded answers through private RAG policy, redacts sensitive values, excludes `apply_remediation`, marks MCP as Technology Preview, can answer the exact Stage 1 Korean custom question, and can route at least 8 of 10 representative Lightspeed questions to the expected OpsLens tool. | Playwright API assertions against `/api/opslens/tools`, `/api/opslens/ask`, JSON-RPC `tools/list` + `tools/call` through `/api/opslens/mcp`, and runtime contract assertions through `/api/opslens/runtime/readiness`; production-facing MCP endpoint is `/mcp`. `npm run verify:lightspeed:trojan-horse` runs the exact prompt `우리 회사 결제 시스템 Pod 장애 대응 매뉴얼 알려줘` through `/mcp` and writes `opslens.lightspeed-trojan-horse.v0.1` evidence. `npm run verify:lightspeed:routing` runs the 10-question fixture and writes `opslens.lightspeed-tool-routing.v0.1` evidence. `npm run verify:lightspeed:integration-handoff` consolidates Trojan Horse proof, routing score, OLSConfig template readiness, live Lightspeed gap classification, read-only follow-up commands, and approval-gated registration commands into `opslens.lightspeed-integration-handoff.v0.1`. `npm run verify:runtime` validates the vLLM/Qdrant readiness contract and can perform opt-in live probes with `--live`; `npm run verify:runtime-rag` validates that `/ask`, MCP `tools/call`, and incident analysis carry the runtime RAG adapter audit with safe local fallback; `npm run verify:runtime-rag:fixture` proves the hybrid success path with mock vLLM/Qdrant endpoints. | Full read-only MCP catalog contains `get_cluster_signal`, `retrieve_customer_knowledge`, `generate_playbook`, `open_console_deep_link`, `run_preflight`, and `propose_remediation`; `apply_remediation` is absent; JSON-RPC `tools/list` marks every MVP tool as `readOnlyHint=true` and `destructiveHint=false`; exact Trojan Horse prompt selects `generate_playbook`, returns Payments customer-runbook citations, redacts a secret-bearing probe, and keeps `clusterMutationAttempted=false`, `vectorWriteAttempted=false`, and `rawDocumentReturned=false`; representative routing score is `selectedPasses>=8/10` and `responsePasses>=8/10`; Lightspeed integration handoff status is `READY_FOR_LIVE_REGISTRATION_REVIEW`, `LIVE_READY`, or explicit `NEEDS_EVIDENCE`, keeps `actionMode=handoffOnly`, separates read-only commands from approval-gated `oc apply`/Operator registration, and keeps mutation flags false; `generate_playbook` returns customer-runbook citations, `rawDocumentReturned=false`, `mutationAllowed=false`, `mcpTechnologyPreview=true`, audit envelope, Qdrant/vLLM endpoint contract, runtime RAG adapter `mode=local` by default, fixture `mode=hybrid` selects runtime citations, `localFallbackUsed=true` by default, `liveProbeEnabled=false` by default, and `NEEDS_LIVE_EVIDENCE` evidence artifact when live runtime probes or retrieval are not requested; `open_console_deep_link` returns Console/OpsLens navigation links with evidence gaps and no mutation; `run_preflight` returns verifier commands, live evidence gaps, and no install/patch execution. | Live Qdrant/vLLM endpoint reachability, live runtime retrieval quality, live model-side Lightspeed tool choice, and live OLSConfig deployment are next Phase 1 implementation lanes. |
| AC-LS-002 | Lightspeed MCP live readiness | Cywell OpsLens validates OpenShift Lightspeed MCP readiness before any OLSConfig mutation. | `npm run verify:lightspeed` reads the live `olsconfigs.ols.openshift.io` CRD through `oc` or the `.env` OCP API fallback, validates `spec.featureGates` and `spec.mcpServers`, reads namespaced or cluster-scoped `OLSConfig/cluster`, checks the template shape, writes `opslens.lightspeed.readiness-evidence.v0.1` to `test-results/cywell-opslens-lightspeed-readiness.json`, and optionally proves `tools/list` + `tools/call` with `--mcp-url` or `CYWELL_OPSLENS_MCP_URL`; `npm run verify:lightspeed:patch-preview` additionally emits a no-apply `opslens.lightspeed.patch-preview.v0.1` artifact from the explicit `PatchOLSConfig` Operator contract, while `npm run verify:lightspeed:patch-preview:fixture` can refresh the same patch-preview artifact from CRD and OLSConfig fixtures when live OCP is unreachable. | Pass/fail/warn verifier output plus machine-readable evidence JSON with branch/head/base/dirty stamp, no cluster apply, no secret values, fixture parity through `npm run verify:lightspeed:fixture`; hard fail if the CRD lacks MCP schema or `--require-mcp` is set without a working MCP endpoint; WARN/`NEEDS_CONFIGURATION` if live feature gate or Cywell MCP registration is not enabled yet; patch preview must preserve existing feature gates/MCP servers, show risk and rollback path, record fixture/live source, and keep `clusterMutationAttempted=false`. | Fixture verification exists locally; live OCP fallback is read-only and does not print secrets; actual MCP registration remains an explicit Operator/OLSConfig patch lane. |
| AC-ENV-001 | OCP/Lightspeed environment isolation | Cywell OpsLens keeps live OCP API connection settings independent from OpenShift Lightspeed/MCP settings so local Lightspeed experiments cannot silently change cluster TLS or timeout behavior. | `npm run verify:env` runs `getOcpConfig()` in an isolated temp cwd with synthetic env values and no real `.env` secrets. | `OCP_API_BASE_URL`/`OCP_API_TOKEN` are used, `OPENSHIFT_LIGHTSPEED_TLS_VERIFY` and `OPENSHIFT_LIGHTSPEED_TIMEOUT_SECONDS` are ignored by OCP config, explicit `OCP_TLS_VERIFY`, `OCP_INSECURE_SKIP_TLS_VERIFY`, `OCP_API_TIMEOUT_SECONDS`, and `OPENSHIFT_API_TIMEOUT_SECONDS` are honored, invalid timeout falls back to 8s. | Real `.env` secrets remain ignored by git and are not printed by the verifier. |
| AC-AIOPS-001 | Alert-triggered incident packet | Cywell OpsLens turns an alert into a plan-only incident packet that combines read-only OCP resource detail, pod candidates, events, recent pod logs, private runbook citations, structured YAML remediation proposal, missing evidence, risks, and rollback path. | Playwright API assertion against `POST /api/opslens/incidents/analyze` after discovering a live pod through `/api/ocp/resources`, plus Alertmanager webhook assertion against `POST /api/opslens/incidents/alertmanager`; pod logs are requested with `sinceSeconds=600`; remediation proposal is asserted as a non-mutating review artifact. `npm run verify:aiops` writes `opslens.aiops-incident-pipeline.v0.1` evidence from the same public API paths and records live gaps as `NEEDS_LIVE_EVIDENCE` instead of claiming readiness. | `actionMode=planOnly`, `opslens.alertmanager-incident-intake.v0.1`, `rawAlertReturned=false`, `clusterMutationAttempted=false`, `policy.mutationAllowed=false`, `policy.serverSideRedaction=true`, redacted log/event evidence, redacted Alertmanager annotations, `analysis.policy.rawDocumentReturned=false`, `analysis.audit.sources`, `analysis.remediationProposal.artifactType=opslens.remediation.proposal.v0.1`, `remediationProposal.mutationAllowed=false`, `remediationProposal.patchType=strategicMerge`, `remediationProposal.triggerEvidence.logs/events/metrics/runbookCitations`, memory proposal `proposedValue=4Gi`, `forbiddenActions=[apply,delete,scale]`, `reviewGate.required=true`, `missingEvidence`, and SelfSubjectAccessReview evidence. | Live remediation execution, history, and apply workflow remain later lanes; the dashboard shows the plan-only review artifact only. |
| AC-AIOPS-002 | Prometheus metric correlation | Incident analysis adds alert, restart, CPU, and memory Prometheus queries when monitoring proxy is enabled, and records explicit missing evidence when it is disabled or unreachable. | Playwright API assertion against the `metrics` block returned by `POST /api/opslens/incidents/analyze`; the same endpoint is valid with `OCP_ENABLE_MONITORING_PROXY=true` or false. `npm run verify:aiops` also checks that the remediation proposal copies metric readiness into `remediationProposal.triggerEvidence.metrics`. | `metrics.windowMinutes=10`, query names include `firing-alert`, `pod-restarts`, `pod-cpu`, `pod-memory`; `policy.monitoringProxyEnabled` mirrors `metrics.enabled`; `remediationProposal.triggerEvidence.metrics.queries` mirrors the same query names and sample status; disabled/unreachable monitoring is surfaced as metric `missingEvidence` instead of hidden. | Full Prometheus charting belongs to the dedicated OpsLens dashboard. |
| AC-RAG-001 | Tenant-scoped local vector RAG | Cywell OpsLens indexes tenant Markdown runbooks into a deterministic local vector index, retrieves customer runbook citations without returning raw documents, redacts sensitive query values, blocks cross-tenant retrieval, and exposes an opt-in Qdrant/vLLM runtime RAG adapter that falls back to local evidence. | `npm run verify:rag` builds `@kugnus/rag`, indexes `data/runbooks`, runs payments and Secret-checklist retrievals, and checks unknown-tenant isolation. `npm run verify:runtime-rag` statically verifies `CYWELL_OPSLENS_RAG_RUNTIME_MODE=local` default safety, vLLM embedding route, Qdrant search route, snippet-only payload mapping, and `/ask`/MCP audit wiring. `npm run verify:runtime-rag:fixture` starts mock vLLM/Qdrant endpoints and proves redacted hybrid retrieval selects runtime citations. | `packages/rag/src/localIndex.ts`, `apps/api/src/runtimeRag.ts`, `scripts/verify-rag-index.mjs`, `scripts/verify-runtime-rag-contract.mjs`, `scripts/verify-runtime-rag-fixture.mjs`, `local-vector-v0.1`, `tenantScoped=true`, `rawDocumentReturned=false`, `serverSideRedaction=true`, chunk-level evidence, `audit.runtimeRag.localFallbackUsed`, fixture `audit.runtimeRag.citationsUsed=runtime`. | The runtime adapter contract and fixture success path exist, but live Qdrant ingestion, vLLM embedding quality, and production vector writes remain evidence-gated later lanes. |
| AC-RAG-002 | Validate-only RAG document intake | Cywell OpsLens accepts operator-authored Markdown runbook drafts only for validation, returns metadata-only document/chunk previews, redacts sensitive-looking text, rejects duplicate tenant document IDs, exports an audit-safe validation artifact, keeps queue submission/inventory separate from ingestion, records human approval/rejection reviews as metadata-only state transitions, produces plan-only ingestion job artifacts without creating ingestion jobs or vector writes, and carries production RAG ingestion readiness as an approval-required handoff contract. | `npm run verify:rag` calls `validateRagDocumentIntake` and `createRagValidationEvidenceExport` with a safe draft containing sensitive-looking text and a duplicate existing runbook ID. `npm run verify:rag:approval-queue` proves default queue submission and inventory stay design-only/read-only, opt-in local persistence writes/lists metadata/redacted evidence only, review decisions update queue metadata, and approved/rejected queue states produce plan-only ingestion artifacts without ingestion. `npm run verify:rag:production-readiness` validates the database-backed queue, ingestion worker, vector write audit sink, source-ref, and rollback export contract without enabling ingestion. Playwright also asserts `POST /api/opslens/admin/rag/validate`, `POST /api/opslens/admin/rag/evidence-export`, `POST /api/opslens/admin/rag/approval-queue/submit`, `GET /api/opslens/admin/rag/approval-queue`, and the dashboard validation/export/queue/inventory panel. | `actionMode=validateOnly`, `policy.uploadApplyAllowed=false`, `rawDocumentReturned=false`, redacted chunk snippets, `sensitive-text` warning, `duplicate-document-id` failure, `opslens.rag.validation-evidence.v0.1`, default `approvalQueue.mode=designOnly`, `enqueueAllowed=false`, `opslens.rag.approval-queue-submission.v0.2`, `opslens.rag.approval-queue-inventory.v0.2`, `opslens.rag.approval-queue-review.v0.1`, `opslens.rag.ingestion-plan.v0.1`, `opslens.rag-production-readiness.v0.1`, `approvalQueueReadOnly`, `approvalReviewOnly`, `ingestionPlanOnly`, `productionReadinessOnly`, default queue `state=design-only`, default inventory `itemCount=0`, opt-in local queue `state=pending-human-approval`, review states `approved-for-ingestion` and `rejected-by-reviewer`, ingestion plan states `blocked` and `ready-for-ingestion-job`, inventory item summary only, `rawMarkdownPersisted=false`, `chunksReturned=false`, `vectorWriteAllowed=false`, `clusterMutationAllowed=false`, `approvalMutationAllowed=false`, `ingestionAllowed=false`, `ingestionJobCreated=false`, `productionQueueEnabledByVerifier=false`, `ingestionWorkerEnabledByVerifier=false`, dashboard `opslens-rag-validation`, `opslens-rag-approval-queue`, `opslens-rag-approval-queue-inventory`, `opslens-rag-approval-review`, and `opslens-rag-ingestion-plan`. | Live production DB-backed queue, live production ingestion worker, and vector DB writes remain approval-required later lanes; the current production readiness contract is non-mutating evidence only. |
| AC-DASH-001 | Dedicated OpsLens admin dashboard | Cywell OpsLens exposes a dashboard surface separate from Lightspeed chat for RAG document health, validate-only RAG draft checks, evidence export, approval queue submission/read-only inventory status, metadata-only approval review, plan-only ingestion job planning, approval-required RAG production handoff readiness, token usage, GPU/runtime samples, Lightspeed MCP tool surface, incident metric visualization, plan-only remediation review, and install readiness. | Playwright API + UI assertions against `GET /api/opslens/admin/overview`, `GET /api/opslens/runtime/readiness`, `POST /api/opslens/admin/rag/validate`, `POST /api/opslens/admin/rag/evidence-export`, `POST /api/opslens/admin/rag/approval-queue/submit`, `GET /api/opslens/admin/rag/approval-queue`, `POST /api/opslens/admin/rag/approval-queue/review`, `POST /api/opslens/admin/rag/approval-queue/ingestion-plan` when local persistence is enabled, and rendered `opslens-admin-dashboard`. | RAG documents are metadata-only and redacted, validation returns `validateOnly`, evidence export returns `markdownReturned=false` and `enqueueAllowed=false`, queue submission renders `state=design-only`, `persisted=false`, and `vectorWrite=false` by default, queue inventory renders `designOnly`, `0 queued`, `readOnly=true`, `vectorWrite=false`, and `approvalMutation=false` by default, persisted queue review renders `approvalReviewOnly`, `queueMetadataWrite=true`, `vectorWrite=false`, and `ingestionJobCreated=false`, ingestion plan renders `ingestionPlanOnly`, `ready-for-ingestion-job` or `blocked`, `vectorWrite=false`, and `ingestionJobCreated=false`, RAG production readiness renders `productionReadinessOnly`, `approval-required`, `contractReady=true`, `queueLive=false`, `workerLive=false`, `vectorAudit=false`, `vectorWrite=false`, and `ingestionJobCreated=false`, token usage has route-level budgets, Lightspeed MCP dashboard matrix renders the same six read-only tools as AC-LS-001 with `/mcp`, `/api/opslens/mcp`, `readOnlyCount=6`, `apply_remediation` excluded, `destructive=false`, `open_console_deep_link` mapped to console navigation, `run_preflight` mapped to install readiness, `propose_remediation` mapped to `planOnly`, the exact Trojan Horse proof from `opslens.lightspeed-trojan-horse.v0.1` with `generate_playbook`, citations, redaction, and `mutationAllowed=false`, and the Lightspeed routing score from `opslens.lightspeed-tool-routing.v0.1` with threshold `8`, GPU samples render, runtime readiness renders `readOnly`, Qdrant/vLLM status, and `liveProbe=false` by default, incident metric query statuses render, `opslens-remediation-proposal` renders YAML `memory: 4Gi` with `mutationAllowed=false` and `reviewGate=true`, Lightspeed MCP readiness is evidence-backed as `ready`, `needs-live-check`, `needs-configuration`, or `failed`, OCP connectivity renders classification and `actionHints` with next read-only checks, Operator dry-run readiness is evidence-backed as `ready`, `partial`, `needs-evidence`, or `failed`, install plan readiness is evidence-backed as `approval-required`, `needs-evidence`, or `failed`, install approval summary renders required approvers, RAG ingestion readiness, `clusterMutationAttempted=false`, `vectorWriteAttempted=false`, `ingestionJobCreated=false`, risk, and rollback path, image build readiness is evidence-backed as `ready`, `needs-evidence`, or `failed`, owned-image provenance readiness is evidence-backed as `ready`, `needs-evidence`, or `failed` and renders required Operator/API/dashboard/bundle inspect status with `readOnlyEvidenceOnly`, `registryMutationAttempted=false`, and `clusterMutationAttempted=false`, external runtime image readiness is evidence-backed as `approval-required`, `needs-evidence`, or `failed`, external runtime summary renders vLLM/Qdrant status, required approvers, candidate matrix status, best candidate critical/high evidence, zero-critical count, `registryMutationAttempted=false`, `clusterMutationAttempted=false`, risk, and rollback path, release publish readiness is evidence-backed as `approval-required`, `needs-evidence`, or `failed`, release publish summary renders required approvers, `registryMutationAttempted=false`, `clusterMutationAttempted=false`, risk, and rollback path, release action queue renders candidate replacement actions with `evidence:external-runtime:candidate-scan`, live evidence handoff renders `handoffOnly`, read-only commands, current gap, forbidden mutating commands, `clusterMutationAttempted=false`, and `registryMutationAttempted=false`, evidence checkpoint readiness is evidence-backed as `ready`, `needs-evidence`, or `blocked`, and the checkpoint summary renders current head, dirty state, lane status, missing evidence, risk, and rollback path. Policy blocks mutation/upload apply/raw document return. | Upload apply, production DB approval queue, live runtime probes, production ingestion worker, and vector DB writes remain later lanes. |
| AC-OP-001 | Operator packaging skeleton | Cywell OpsLens defines an explicit Operator install contract for API, dashboard, vector store, model runtime, validate-only RAG document intake, design-only RAG approval queue, live-schema ConsolePlugin, ingress NetworkPolicies, opt-in Lightspeed MCP registration, and a Go/controller-runtime manager source path for explicit OLSConfig patching. | `npm run verify:operator` parses Operator YAML, validates CRD/sample/CSV/RBAC/bundle metadata/app manifests, checks `ValidateOnly` default, explicit `PatchOLSConfig` sample registration, RAG `rawDocumentReturnAllowed=false`, approval `enqueueAllowed=false`, denies Operator raw Secret read RBAC, verifies ConsolePlugin `spec.backend`, `proxy.authorization=UserToken`, `proxy.endpoint`, OpenShift service-ca annotations, HTTPS service ports, TLS secret mounts, API/dashboard ingress NetworkPolicies for Console/Lightspeed traffic, and statically checks `deploy/operator/controller-runtime/**` including the OLSConfig patch source. | `deploy/operator/config/**`, `deploy/operator/bundle/**`, `deploy/operator/controller-runtime/**`, `OpsLensInstallation.spec.rag`, `cywell-opslens-rag-policy` ConfigMap, API/dashboard TLS env and readiness probes, `ClusterServiceVersion`, `ConsolePlugin` backend/proxy on HTTPS 443, `cywell-opslens-api-ingress`, `cywell-opslens-dashboard-ingress`, managed `OLSConfig` MCP URL ending `/mcp`, RBAC for `olsconfigs`, `consoleplugins`, `networkpolicies`, service account reconciliation, and no `secrets get/list/watch`. | Live OLM install/upgrade/uninstall smoke, actual image push, and external certification pipeline execution are next Stage 4 lanes. |
| AC-OP-002 | Operator reconcile safety core | Cywell OpsLens controller logic separates non-mutating `ValidateOnly` from explicit `PatchOLSConfig`, preserves existing Lightspeed config state, emits rollback path/evidence, keeps assistant actions plan-only, renders RAG approval queue policy as design-only/non-mutating, and mirrors the ConsolePlugin HTTPS/UserToken plus ingress NetworkPolicy contract in controller-runtime source. | `npm run verify:operator:reconcile` builds `@kugnus/operator-controller` and runs fixture assertions against base, registered, missing, and validate-only OLSConfig states plus RAG policy rendering. `npm run verify:operator:runtime` compares the TypeScript desired resource plan with the Go/controller-runtime source for ServiceAccount, service-ca HTTPS Services, runtime ports, TLS env/mounts, vector persistence, ConsolePlugin backend/proxy schema, ingress NetworkPolicies, RAG env/status safety, OLSConfig patch path, and RBAC parity. | `packages/operator-controller/src/reconcile.ts`, `deploy/operator/controller-runtime/controllers/opslensinstallation_controller.go`, `deploy/operator/fixtures/*.yaml`, `scripts/verify-operator-runtime-parity.mjs`, `strategicMergePatch.spec.featureGates`, preserved existing MCP servers, Cywell `/mcp` registration, Go `client.MergeFrom(original)` patch, rollback annotation, `mutationAllowed=false` for ValidateOnly, `ragApprovalQueueMutationAllowed=false`, `ragRawDocumentReturnAllowed=false`, `MissingOLSConfig` blocks patching, `ConsolePlugin.spec.backend`, UserToken API proxy endpoint, `openshift-console`/`openshift-lightspeed` namespace ingress on TCP 9443. | Local Go toolchain is unavailable, so Go formatting/compile is verified through container image build gates; live Operator SDK runtime and OLM install smoke remain Stage 4 follow-up. |
| AC-OP-003 | Console dynamic plugin assets | Cywell OpsLens dashboard image emits real OpenShift Console dynamic plugin assets and routes dashboard API calls through the Console plugin UserToken proxy when loaded in-console. | `npm run build` generates the Vite dashboard plus Webpack/ConsoleRemotePlugin assets; `npm run verify:console-plugin` validates `plugin-manifest.json`, `plugin-entry.js`, exposed route chunk, navigation extensions, `/opslens` route, iframe dashboard asset URL, `apiBase` proxy routing, and asset MIME support. | `apps/web/package.json` `consolePlugin`, `apps/web/console-extensions.json`, `apps/web/src/plugin/OpsLensRoute.tsx`, `apps/web/src/lib/api.ts`, `apps/web/scripts/serve.mjs`, `apps/web/dist/plugin-manifest.json`, `apps/web/dist/plugin-entry.js`, `test-results/cywell-opslens-console-plugin-assets.json`. | Live console Operator enablement and browser-in-console smoke remain Stage 4 follow-up. |
| AC-OP-004 | Live Operator server-side dry-run | Cywell OpsLens can validate Operator CRD/RBAC/manager/app manifests against a live OpenShift API without mutating the cluster or printing secrets. | `npm run verify:operator:dry-run` parses the install manifests, uses `oc apply --dry-run=server --validate=true` against the configured `.env` OCP API credentials or current `oc` context, writes `opslens.operator.server-dry-run.v0.1`, marks `clusterMutationAttempted=false`, and reports skipped namespaced resources when the target namespace does not exist yet. | `scripts/verify-operator-dry-run.mjs`, `test-results/cywell-opslens-operator-dry-run.json`, `branch/head/base/dirty` stamp, sanitized `oc` output, accepted live API resources, skipped missing-namespace resources, missing evidence, risk, and rollback path. | This is non-mutating preflight only; live OLM install, upgrade, rollback, uninstall, image pull, and pod readiness smoke remain explicit Stage 4 mutating gates. |
| AC-OP-005 | Mutating install approval plan | Cywell OpsLens generates an explicit human approval plan before any live OLM install, OLSConfig mutation, or future RAG ingestion worker. | `npm run verify:install-plan` parses CatalogSource, Manual Subscription, and `OpsLensInstallation` sample manifests, reads MVP, Operator dry-run, Lightspeed readiness, Lightspeed patch preview, image readiness, and RAG approval queue evidence, requires a clean current worktree, requires all required approval inputs to be same-HEAD, requires `verify:images:build` actual-build evidence, and requires `verify:rag:approval-queue` ingestion-plan evidence before approval-ready status, writes `opslens.install-approval-plan.v0.1`, exposes `lightspeedRegistration` as a `previewOnly` OLSConfig/PatchOLSConfig registration summary with read-only preview commands and `legacyConfigMapMutationAttempted=false`, and keeps `clusterMutationAttempted=false`, `vectorWriteAttempted=false`, and `ingestionJobCreated=false`. `npm run verify:evidence-checkpoint` then verifies that the install/release/runtime evidence chain, RAG approval queue bridge, RAG production readiness handoff, RAG ingestion boundary, and Lightspeed routing score are current-head and classifies remaining live/external blockers without mutating anything. | `scripts/verify-install-approval-plan.mjs`, `scripts/verify-evidence-checkpoint.mjs`, `test-results/cywell-opslens-install-approval-plan.json`, `test-results/cywell-opslens-evidence-checkpoint.json`, `test-results/cywell-opslens-rag-approval-queue.json`, `test-results/cywell-opslens-rag-production-readiness.json`, `test-results/cywell-opslens-lightspeed-tool-routing.json`, required approvers, Lightspeed registration target, desired MCP server, read-only patch preview command, RAG ingestion required approvers, preflight commands, mutating commands marked `requiresExplicitApproval=true`, non-mutating verification commands marked `mutation=false`, current-head MVP/dry-run/RAG approval queue/RAG production readiness/Lightspeed routing/readiness/image/release/runtime evidence, missing evidence, risk, rollback path, and branch/head/base/dirty stamp. | Actual namespace creation, CatalogSource/Subscription apply, InstallPlan approval, `OpsLensInstallation` apply, pod readiness smoke, OLSConfig patch, production RAG ingestion worker, and vector writes remain blocked until explicit human approval. |
| AC-CERT-001 | Catalog and certification readiness draft | Cywell OpsLens prepares internal catalog, FBC, Manual Subscription, scorecard config, Red Hat-oriented annotations, support matrix, security readiness, catalog toolchain readiness, image build contracts, owned-image provenance, external runtime certification/mirroring approval, release publish approval, release evidence bundle, release action queue, and release gates without claiming certification has passed. | `npm run verify:certification` parses CSV/bundle metadata/FBC/CatalogSource/Subscription/scorecard/docs and reports missing external CLIs or placeholders as warnings; `npm run verify:catalog-toolchain` validates CSV/FBC/CatalogSource/Subscription/scorecard parity, records `docker`/`opm`/`operator-sdk`/`podman`/`oc` availability plus registry.redhat.io auth presence and read-only base-image manifest readability without exporting credentials, separates read-only validation, human setup, local artifact build, and approval-gated publish commands, then writes `opslens.catalog-toolchain-plan.v0.1`; `npm run verify:images` checks Operator/API/dashboard/bundle/catalog Dockerfile readiness against CSV `relatedImages` and writes `opslens.image-build-readiness.v0.1`; `npm run verify:images:build` optionally performs local Docker builds without pushing images and records registry-auth catalog gaps as warnings; `npm run verify:owned-image-provenance` reads local Docker image metadata for Operator/API/dashboard/bundle/catalog, requires same-HEAD actual build evidence for required owned images, writes `opslens.owned-image-provenance.v0.1`, and keeps registry/cluster mutation false; `npm run verify:release-refresh -- --skip-image-build` runs `verify:mvp -- --skip-images` so the MVP gate cannot downgrade release image evidence, then preserves or reports same-HEAD actual build evidence through the dedicated image/provenance gates; `npm run evidence:external-runtime:draft -- --all` creates ignored `*.draft.json` reviewer packets only, `npm run evidence:external-runtime:draft:digests` additionally performs read-only manifest inspection to fill available source digests, both reject secret-like values, record ref stamps and no-mutation flags, and cannot satisfy final readiness by themselves; `npm run evidence:external-runtime:candidates` writes a non-mutating candidate matrix that compares current vLLM/Qdrant scan/SBOM evidence with scanned alternatives, records best candidate, remaining critical/high findings, zero-critical candidates, missing evidence, risk, and rollback path without changing release manifests; `npm run evidence:external-runtime:review-packet` writes a JSON/Markdown reviewer packet with vLLM/Qdrant draft state, source digest inspection, candidate matrix state, reviewer requests, missing evidence, read-only refresh commands, and approval-gated mirror/sign commands that were not run; `npm run evidence:external-runtime:promote -- --name vllm|qdrant --promote-reviewed --reviewer <reviewer> --review-ticket <ticket>` refuses incomplete drafts and is the only scripted path to final reviewed external runtime evidence; `npm run verify:external-runtime-plan` checks vLLM/Qdrant image references across CSV, FBC, CRD, samples, fixtures, and app manifests, surfaces draft intake status if present, then writes a no-mirror `opslens.external-runtime-images-plan.v0.1` artifact with certification, scan, SBOM, provenance, mirror digest, approval, risk, and rollback requirements; `npm run verify:release-plan` requires a clean current worktree, same-HEAD image build evidence, same-HEAD owned-image provenance evidence, and same-HEAD external runtime plan evidence, then writes a no-push `opslens.release-publish-plan.v0.1` approval artifact for push/sign/mirror/catalog publication; `npm run verify:release-evidence-bundle` reads the current checkpoint, roadmap, release, install, live handoff, OCP network handoff, certification readiness, catalog toolchain, image, provenance, external runtime plan, and external runtime review packet artifacts into one non-mutating `opslens.release-evidence-bundle.v0.1` packet for release-manager review; `npm run evidence:release-action-queue` converts checkpoint, release bundle, network handoff, external runtime review, release plan, and install plan gaps into owner-scoped blocker/high actions without running approval-gated commands. | `deploy/catalog/**`, `deploy/operator/bundle/tests/scorecard/config.yaml`, `apps/api/Dockerfile`, `apps/web/Dockerfile`, `deploy/operator/controller-runtime/Dockerfile`, `deploy/operator/controller-runtime/go.sum`, CSV/FBC `relatedImages`, CRD/sample/fixture/app manifest runtime references, `com.redhat.openshift.versions`, `features.operators.openshift.io/*`, support/security/release docs, `installPlanApproval: Manual`, `docs/release/evidence/external-runtime/*.example.json`, ignored draft intake files, `test-results/cywell-opslens-certification-readiness.json`, `test-results/cywell-opslens-catalog-toolchain-plan.json`, `test-results/cywell-opslens-owned-image-provenance.json`, `test-results/cywell-opslens-external-runtime-images-plan.json`, `test-results/cywell-opslens-external-runtime-candidate-matrix.json`, `test-results/cywell-opslens-external-runtime-candidate-matrix.md`, `test-results/cywell-opslens-external-runtime-review-packet.json`, `test-results/cywell-opslens-external-runtime-review-packet.md`, `test-results/cywell-opslens-release-publish-plan.json`, `test-results/cywell-opslens-release-evidence-bundle.json`, `test-results/cywell-opslens-release-action-queue.json`, `test-results/cywell-opslens-release-action-queue.md`. | `opm`, `operator-sdk`, final vLLM/Qdrant certification evidence, external runtime vulnerability/SBOM/provenance evidence, partner portal submission, actual image push/mirror/sign, immutable registry repo digest/signature/SBOM/scan evidence, catalog image build/release review on a clean authenticated toolchain, and live certification pipeline execution are external follow-up gates. |
| AC-OCP-001 | Live OCP read-only discovery | API discovers live OpenShift resources, renders console-like overview, proves read access with `SelfSubjectAccessReview`, exposes `get/list/watch` matrix, generates bounded/full resource coverage matrices with gap classification, diagnoses CRD/APIService/conversion-webhook failures, probes alternate served API versions, exports evidence, lists paginated and label/field selector-scoped pods, supports namespace selection, reads sanitized pod detail as JSON/YAML, resolves owner/child relationships, reads pod logs/events, blocks raw Secret fetch, and classifies API reachability before live checks are trusted. | Playwright API + UI assertions against configured CRC/OpenShift local, including list/detail fallback when diagnostic finds a readable alternate served version. `npm run verify:ocp:connectivity` writes a read-only DNS/TCP/TLS/API/oc diagnostic artifact for the configured `.env` or kubeconfig target. | `/api/ocp/status`, `/api/ocp/console-overview`, `/api/ocp/api-resources`, `/api/ocp/access-review`, `/api/ocp/access-matrix`, `/api/ocp/coverage-matrix`, `/api/ocp/coverage-diagnostic`, `/api/ocp/resources?labelSelector=...&fieldSelector=...`, `/api/ocp/resource`, `/api/ocp/related`, `/api/ocp/pod-logs`, `/api/ocp/events`; fallback response blocks with requested/served API version and evidence; `opslens.ocp-connectivity-diagnostic.v0.1` records redacted target, token configured state, DNS, TCP, TLS, `/version`, `oc`, classification, action hints, Windows-friendly read-only troubleshooting commands such as `Test-NetConnection`/`Resolve-DnsName`/`route print`, missing evidence, risk, rollback path, and `clusterMutationAttempted=false`. | Monitoring proxy is opt-in; bounded coverage reports skipped resources as `not-probed`; cluster API/webhook failures remain visible as classified diagnostic gaps; readable alternate versions are evidence-backed fallbacks, not hidden repairs; mutating verbs and raw Secrets remain out of scope. |
| AC-OCP-RBAC-001 | OCP fallback reader RBAC approval packet | Cywell OpsLens keeps normal live reads on user-token passthrough, and when shared diagnostic evidence needs a fallback credential it produces a least-privilege, cluster-admin-reviewable Namespace/ServiceAccount/ClusterRole/ClusterRoleBinding plan separate from Operator controller RBAC. The plan grants only `get/list/watch`, excludes Secrets, records citation/evidence gaps, and never applies RBAC or creates tokens from the verifier. | `npm run evidence:ocp-auth-rbac-plan` parses `deploy/ocp-live-readonly/opslens-live-evidence-reader.yaml` with `yaml`, validates Namespace/ServiceAccount/ClusterRole/ClusterRoleBinding names, checks verbs/resources, reads the redacted OCP connectivity classification, writes JSON and Markdown evidence, and feeds checkpoint, roadmap, release bundle, action queue, API, and dashboard. After cluster-admin approval and approved short-lived token handling, `npm run verify:ocp:live-reader-smoke -- --timeout-ms 30000` reruns OCP connectivity and Lightspeed readiness as a read-only post-approval smoke without creating tokens or printing secrets. | `deploy/ocp-live-readonly/opslens-live-evidence-reader.yaml`, `scripts/verify-ocp-auth-rbac-plan.mjs`, `scripts/verify-ocp-live-reader-smoke.mjs`, `test-results/cywell-opslens-ocp-auth-rbac-plan.json`, `test-results/cywell-opslens-ocp-auth-rbac-plan.md`, `test-results/cywell-opslens-ocp-live-reader-smoke.json`, `actionMode=approvalPlanOnly`, `clusterMutationAttempted=false`, `registryMutationAttempted=false`, `mutationAllowedByThisVerifier=false`, `rbac.namespace.name=cywell-opslens`, `rbac.clusterRole.readOnlyOnly=true`, `rbac.clusterRole.secretsIncluded=false`, read-only dry-run/`oc auth can-i` commands, post-approval smoke command, approval-gated `oc apply` and short-lived token commands not run, dashboard `Auth/RBAC Plan`, checkpoint lane `ocpAuthRbacPlan`, and action queue owner item `cluster-admin-approve-ocp-live-reader-rbac`. | Actual namespace/RBAC application, token creation, credential rotation, and credential storage are cluster-admin/security approval tasks; automatic apply/delete/scale and raw Secret reads remain out of scope. |
| AC-LIVE-HANDOFF-001 | Live evidence handoff | Cywell OpsLens creates an SRE-safe handoff artifact that turns current OCP/Lightspeed/install evidence gaps into an ordered read-only command chain without approving mutation. | `npm run verify:live-handoff` reads OCP connectivity, OCP auth/RBAC plan, optional post-approval live reader smoke, Operator dry-run, Lightspeed readiness, Lightspeed patch preview, install approval plan, and checkpoint artifacts, then writes `opslens.live-evidence-handoff.v0.1`. `npm run evidence:ocp-network-handoff` additionally writes a redacted JSON and Markdown packet for Network/SRE review when OCP reachability is blocked. Playwright asserts the dashboard exposes live handoff, post-approval smoke RBAC counts, source artifact freshness, verifier run status, network handoff, and auth/RBAC summaries. | `test-results/cywell-opslens-live-evidence-handoff.json`, `test-results/cywell-opslens-ocp-live-reader-smoke.json`, `test-results/cywell-opslens-ocp-network-handoff.json`, `test-results/cywell-opslens-ocp-network-handoff.md`, `actionMode=handoffOnly`, `readOnlyCommands` including OCP network troubleshooting commands and `ocp-live-reader-smoke`, `postApprovalSmoke.requiredRbacReviewCount`, `requiredRbacAllowedCount`, `requiredRbacUnknownCount`, `lightspeedClassification`, `sourceArtifacts`, `verifierRuns`, current gap classification, OCP action hints, forbidden commands include `oc apply`, `clusterMutationAttempted=false`, `registryMutationAttempted=false`, `mutationAllowedByThisVerifier=false`, secret redaction, dashboard `Network Handoff` readiness, and checkpoint lanes `liveHandoff` and `ocpNetworkHandoff`. | The handoff does not run live checks by itself, install Operators, patch OLSConfig, push images, mirror runtime images, create credentials, or replace the human approval plan. |

## Evidence Chain Extension

- AC-DASH-001 includes dashboard panels for certification readiness, catalog toolchain readiness, ConsolePlugin asset readiness, external runtime reviewer packet readiness, external runtime candidate matrix readiness, security scan/SBOM readiness, release evidence refresh, release evidence bundle readiness, and release action queue readiness. The API/UI must expose `certificationReadinessOnly`, `toolchainPlanOnly`, ConsolePlugin source artifacts, `reviewPacketOnly`, candidate matrix status, best replacement candidate evidence, zero-critical count, `scanPlanOnly`, `localEvidenceRefresh`, `bundleOnly`, `actionQueueOnly`, CLI/tooling gaps, same-HEAD freshness, command status, decision state, owner action summary, mutation flags, risk, rollback path, and missing evidence without submitting to Partner Connect, publishing catalog images, promoting drafts, mirroring/signing runtime images, or applying cluster resources. The consolidated checkpoint and roadmap Stage 3/4 must carry `consolePluginAssets`, and Stage 5 must carry `certificationReadiness`, `catalogToolchain`, `externalRuntimeReviewPacket`, and `securityScan` so release review sees local tooling gaps instead of relying on dashboard-only evidence.
- AC-DASH-001 also requires the AI Ops pipeline dashboard card to expose Alertmanager webhook intake evidence from `npm run verify:aiops`, including `opslens.alertmanager-incident-intake.v0.1`, accepted alert count, `rawAlertReturned=false`, `clusterMutationAttempted=false`, and `mutationAllowed=false`.
- AC-DASH-001 requires the release refresh panel to expose action queue owner packet readiness after `npm run verify:release-refresh`, including owner packet count, missing owner packet evidence, and file existence for generated owner-scoped handoff packets.
- AC-DASH-001 also requires the install approval panel to render `approvalPlan.lightspeedRegistration` with `previewOnly`, `configResourceKind=OLSConfig`, `mode=PatchOLSConfig`, the target OLSConfig, desired `/mcp` server, the read-only patch-preview command, `legacyConfigMapMutationAttempted=false`, and `clusterMutationAttempted=false`.
- AC-DASH-001 also requires the Lightspeed MCP dashboard card to render the integration handoff from `npm run verify:lightspeed:integration-handoff`, including `handoffOnly`, status, live/network classification, OLSConfig template readiness, read-only command count, approval-gated command count, and mutation flags without running `oc apply`, Operator patching, vector writes, or ingestion jobs.
- AC-CERT-001 image readiness also verifies that API/dashboard Dockerfiles use `COPY --chown=1001:0` plus non-root build/runtime steps instead of recursive `chown -R`/`chmod -R`, so release evidence refresh can rebuild owned images without spending minutes walking `node_modules`.
- `npm run verify:roadmap-plan` writes `opslens.roadmap-plan-alignment.v0.1` JSON plus Markdown, maps `kugnus-idea/CywellOpsLens_plan.md` stages 1-5 to current same-HEAD evidence, and blocks legacy Lightspeed ConfigMap mutation language so Stage 4 remains the explicit `OLSConfig`/`PatchOLSConfig` approval path.
- AC-CERT-001 treats `npm run verify:certification`, `npm run verify:catalog-toolchain`, `npm run verify:security-scan-plan`, and `npm run verify:release-refresh` as review inputs for release-manager evidence packets; they do not replace final Red Hat certification, external runtime SBOM/scan/provenance, registry digest/signature, or human approval evidence.
- `npm run verify:certification` writes `opslens.certification-readiness.v0.1`, records Community/Certified Operator packaging/doc readiness plus local `opm`/`operator-sdk` tooling gaps, emits a structured `toolingHandoff` with `humanSetupOnly`, read-only validation commands, human setup commands, approval-gated external submission commands, and next commands, verifies the certification tooling handoff document, and feeds checkpoint, roadmap Stage 5, release bundle, and release action queue evidence.
- `docs/release/evidence/external-runtime/README.md` defines external runtime reviewer roles, evidence state transitions from `example-only` to `reviewed-final`, and approval-gated mirror/sign boundaries; `npm run verify:external-runtime-plan` fails if this intake contract drifts.
- `npm run verify:security-scan-plan` writes `opslens.security-scan-plan.v0.1`, checks local scan/SBOM/signing CLI readiness, consumes same-HEAD Docker fallback runner coverage when present, lists missing vulnerability/SBOM/security-review files, separates read-only evidence generation from approval-gated `cosign sign`, and feeds checkpoint, roadmap Stage 5, and release bundle evidence. `npm run evidence:security-scan:docker` or `npm run verify:release-refresh -- --security-scan-docker` may generate owned-image vulnerability/SBOM files through digest-resolved scanner containers when local `trivy`/`syft` CLIs are unavailable, but they still create only draft review packets and no final human approval.
- `npm run evidence:external-runtime:candidate-scan` wraps the security scan runner for a single vLLM/Qdrant candidate image, carries scanner timeout/scanner-mode options for large runtime images, writes candidate evidence under `test-results/security-candidates/<name>-<candidate-label>`, refreshes the candidate matrix after executed scans, and keeps CSV/FBC/runtime manifests unchanged. `npm run evidence:external-runtime:candidates` writes `opslens.external-runtime-candidate-matrix.v0.1`, compares scanned external runtime alternatives against current release scan evidence, and feeds the review packet/release bundle as local reviewer evidence only.
- `npm run evidence:external-runtime:review-packet` writes `opslens.external-runtime-review-packet.v0.1`, consolidates vLLM/Qdrant reviewer requests, reviewer next commands, candidate matrix state, and approval-gated runtime commands, and feeds checkpoint, roadmap Stage 5, release bundle, release action queue, API, and dashboard evidence without promoting drafts or running mirror/sign/push commands.
- `npm run verify:release-evidence-bundle` writes `test-results/cywell-opslens-release-evidence-bundle.json` plus `test-results/cywell-opslens-release-evidence-bundle.md`; API/UI must surface the Markdown packet path as `bundle.markdownPath` while preserving `bundleOnly`, no-mutation flags, missing evidence, risk, rollback, and approval-gated command boundaries.
- `npm run evidence:release-action-queue` writes `opslens.release-action-queue.v0.1` JSON plus Markdown plus owner-scoped Markdown packets under `test-results/release-action-queue-owners/`, groups checkpoint/release/network/external-runtime/runtime-RAG gaps by operational owner, routes OCP network handoff/connectivity gaps with structured diagnostics for `ocp-network-target`, DNS/probe status, read-only commands, RBAC readiness, and mutation boundary, surfaces candidate-matrix replacement-image actions with `evidence:external-runtime:candidate-scan` plus structured candidate diagnostics for status, best candidate, zero-critical findings, current-to-candidate delta, SBOM package count, and promotion approval boundary, catalog registry-auth actions with `verify:catalog-toolchain`, runtime live evidence actions with `verify:runtime -- --live` plus structured runtime RAG diagnostics that separate contract `NEEDS_LIVE_EVIDENCE`, fixture `PASS`, live vLLM/Qdrant missing evidence, and read-only fallback boundary, routes RAG production ingestion gaps to `rag-owner-enable-production-approval-queue` with `verify:rag:production-readiness`, queue/worker/audit diagnostics, and approval-gated commands not run, and security-review actions with structured diagnostics for final review state, draft state, scan/SBOM evidence, reviewer, and ticket as next read-only evidence commands, lists read-only refresh commands and approval-gated commands not run, exposes `ownerPackets` through the API/dashboard, and feeds the dashboard as `actionQueueOnly` evidence with `actionQueue.markdownPath` without approving push, mirror, sign, install, patch, apply, delete, or scale.
- Lightspeed readiness gaps are owner-routed from `opslens.lightspeed.readiness-evidence.v0.1`: `auth-or-rbac`, `auth-failed`, and `token-missing` become `cluster-admin-fix-lightspeed-readiness-auth-rbac` with `lightspeed-readiness-live` as read-only follow-up, post-approval live reader smoke diagnostics (`requiredRbacReviewCount`, allowed/denied/unknown counts, source artifact freshness, verifier run status), and the OCP auth/RBAC approval commands preserved as approval-gated, while TLS/network classifications route to Cluster SRE or Network SRE.
- The release action queue routes AI Ops Prometheus/monitoring proxy gaps from `opslens.aiops-incident-pipeline.v0.1` to `cluster-sre-enable-monitoring-proxy-evidence` with `aiops-monitoring-proxy-smoke` as a read-only follow-up; the dashboard must expose this as a monitoring proxy action without enabling the proxy or mutating the cluster.
- OCP handoff/action queue routing is classification-aware: `tcp-timeout`, `tcp-unreachable`, and DNS gaps route to Network/SRE; `tls-handshake-failed` routes to Cluster SRE/Security; `auth-or-rbac`, `auth-failed`, and `token-missing` route to Cluster Admin/SRE for credential or read-only RBAC evidence, and the auth/RBAC blocker next command points at `npm run evidence:ocp-auth-rbac-plan` before the live readiness rerun.

## Explicit Non-Goals

- No automatic `apply`, `delete`, or `scale`.
- No Kubernetes Secret fetch.
- No external LLM provider call from the prototype.
- No full Operator controller lifecycle yet; MVP 0.1 includes package contract, controller-runtime source with explicit OLSConfig patch path, TypeScript reconcile contract, and static verifier.
- No production RAG/vector database yet; Stage 1 exposes the contract, local Markdown private-RAG policy path, validate-only intake checks, and evidence export artifact.

## Verification Command

```bash
npm run verify:mvp
npm run build
npm run test:e2e
npm run verify:rag
npm run verify:runtime
npm run verify:runtime-rag
npm run verify:runtime-rag:fixture
npm run verify:aiops
npm run verify:lightspeed:trojan-horse
npm run verify:lightspeed:routing
npm run verify:lightspeed:integration-handoff
npm run verify:operator
npm run verify:operator:dry-run
npm run verify:install-plan
npm run verify:operator:reconcile
npm run verify:operator:runtime
npm run verify:certification
npm run verify:catalog-toolchain
npm run verify:owned-image-provenance
npm run verify:external-runtime-plan
npm run verify:lightspeed:patch-preview:fixture
npm run verify:release-plan
npm run verify:release-refresh
npm run verify:release-evidence-bundle
npm run verify:evidence-checkpoint
npm run verify:lightspeed:fixture
```
