apiVersion: scorecard.operatorframework.io/v1alpha3
kind: Configuration
metadata:
  name: cywell-opslens-scorecard
stages:
  - parallel: true
    tests:
      - image: quay.io/operator-framework/scorecard-test:v1.39.0
        entrypoint:
          - scorecard-test
          - basic-check-spec-test
        labels:
          suite: basic
          test: basic-check-spec-test
      - image: quay.io/operator-framework/scorecard-test:v1.39.0
        entrypoint:
          - scorecard-test
          - olm-bundle-validation-test
        labels:
          suite: olm
          test: olm-bundle-validation-test
